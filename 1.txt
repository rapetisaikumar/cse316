#include<sys/wait.h>
#include<sys/types.h>
#include<stdio.h>
#include<stdlib.h>
int main()
{
pid_t p;
int n,i;
p=fork();
if(p>0)
{
printf(" parent process \n");

}
else if(p==0)
{
printf(" child process\n" );
printf(" enter the number\n" );
scanf("%d",&n);
if(n>0)
{
}
else
{
}
